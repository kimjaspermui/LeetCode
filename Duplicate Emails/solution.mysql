# Write your MySQL query statement below

# select unique emails where p1 and p2 has same email but different id
SELECT DISTINCT p1.Email
FROM Person as p1, Person as p2
WHERE p1.Email = p2.Email AND p1.Id != p2.Id;
